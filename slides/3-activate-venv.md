# 3 — Activate the prepared environment

A Python environment (`.venv`) is already prepared in this folder.

Turn it on:

```powershell
.\.venv\Scripts\Activate.ps1
```

You'll know it worked when your prompt starts with **`(.venv)`**.
